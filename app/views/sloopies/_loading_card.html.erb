<h2><%= sloopy.origin.capitalize %> - <%= sloopy.destination.capitalize %></h2>
<%= render "shared/loader",
  container_id: "loader_#{sloopy.id}",
  container_class: "sloopy-loader",
  hint: "This may take a few moments..."
%>
